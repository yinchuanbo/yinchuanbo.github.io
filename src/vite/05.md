---
title: 静态资源处理
category: Vite7.0
date: 2025-07-31 12:54:44
---

Vite 提供了强大的静态资源处理能力，支持多种资源类型的导入、优化和处理。本章将详细介绍如何在 Vite 中处理各种静态资源。

## 资源导入方式

### 基本资源导入
```javascript
// 导入图片资源
import logoUrl from './assets/logo.png'
import iconSvg from './assets/icon.svg'

// 在组件中使用
function App() {
  return (
    <div>
      <img src={logoUrl} alt="Logo" />
      <img src={iconSvg} alt="Icon" />
    </div>
  )
}
```

### 显式 URL 导入
```javascript
// 显式导入为 URL
import workletURL from 'extra-scalloped-border/worklet.js?url'

// 导入为字符串
import shaderString from './shader.glsl?raw'

// 导入为 Web Worker
import Worker from './worker.js?worker'

// 导入为内联 Worker
import InlineWorker from './worker.js?worker&inline'
```

### 动态资源导入
```javascript
// 动态导入图片
function getImageUrl(name) {
  return new URL(`./assets/images/${name}.png`, import.meta.url).href
}

// 使用示例
const logoUrl = getImageUrl('logo')
const iconUrl = getImageUrl('icon')
```

## 图片资源处理

### 基础图片导入
```javascript
// src/components/ImageGallery.vue
<template>
  <div class="gallery">
    <img :src="heroImage" alt="Hero" class="hero" />
    <div class="thumbnails">
      <img 
        v-for="(thumb, index) in thumbnails" 
        :key="index"
        :src="thumb" 
        alt="Thumbnail"
        class="thumbnail"
      />
    </div>
  </div>
</template>

<script>
import heroImage from '@/assets/images/hero.jpg'
import thumb1 from '@/assets/images/thumb1.jpg'
import thumb2 from '@/assets/images/thumb2.jpg'
import thumb3 from '@/assets/images/thumb3.jpg'

export default {
  data() {
    return {
      heroImage,
      thumbnails: [thumb1, thumb2, thumb3]
    }
  }
}
</script>
```

### 响应式图片
```javascript
// 导入不同尺寸的图片
import heroSmall from '@/assets/images/hero-small.jpg'
import heroMedium from '@/assets/images/hero-medium.jpg'
import heroLarge from '@/assets/images/hero-large.jpg'

// 创建响应式图片组件
function ResponsiveImage({ alt, className }) {
  return (
    <picture className={className}>
      <source 
        media="(max-width: 768px)" 
        srcSet={heroSmall} 
      />
      <source 
        media="(max-width: 1200px)" 
        srcSet={heroMedium} 
      />
      <img src={heroLarge} alt={alt} />
    </picture>
  )
}
```

### 图片优化配置
```javascript
// vite.config.js
import { defineConfig } from 'vite'
import { ViteImageOptimize } from 'vite-plugin-imagemin'

export default defineConfig({
  plugins: [
    ViteImageOptimize({
      // JPEG 优化
      mozjpeg: {
        quality: 80,
        progressive: true
      },
      // PNG 优化
      pngquant: {
        quality: [0.65, 0.8],
        speed: 4
      },
      // SVG 优化
      svgo: {
        plugins: [
          { name: 'removeViewBox', active: false },
          { name: 'removeEmptyAttrs', active: false },
          { name: 'removeUselessStrokeAndFill', active: false }
        ]
      },
      // WebP 转换
      webp: {
        quality: 75
      }
    })
  ],
  
  build: {
    // 小于 4kb 的图片将被内联为 base64
    assetsInlineLimit: 4096
  }
})
```

## SVG 资源处理

### SVG 作为图片使用
```javascript
// 直接导入 SVG
import logoSvg from './assets/logo.svg'

function Header() {
  return (
    <header>
      <img src={logoSvg} alt="Logo" width="120" height="40" />
    </header>
  )
}
```

### SVG 作为组件使用
```javascript
// 使用 vite-plugin-svgr
import { defineConfig } from 'vite'
import svgr from 'vite-plugin-svgr'

export default defineConfig({
  plugins: [svgr()]
})

// 在组件中使用
import { ReactComponent as LogoIcon } from './assets/logo.svg'

function Header() {
  return (
    <header>
      <LogoIcon className="logo" />
    </header>
  )
}
```

### 内联 SVG
```javascript
// 导入 SVG 为字符串
import iconSvg from './assets/icon.svg?raw'

function IconButton() {
  return (
    <button 
      className="icon-btn"
      dangerouslySetInnerHTML={{ __html: iconSvg }}
    />
  )
}
```

## 字体资源处理

### 字体文件导入
```css
/* src/styles/fonts.css */
@font-face {
  font-family: 'CustomFont';
  src: url('./assets/fonts/custom-font.woff2') format('woff2'),
       url('./assets/fonts/custom-font.woff') format('woff');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

.custom-text {
  font-family: 'CustomFont', Arial, sans-serif;
}
```

### 字体预加载
```html
<!-- index.html -->
<head>
  <link 
    rel="preload" 
    href="/src/assets/fonts/custom-font.woff2" 
    as="font" 
    type="font/woff2" 
    crossorigin
  >
</head>
```

### 动态字体加载
```javascript
// src/utils/fontLoader.js
export async function loadFont(fontName, fontUrl) {
  const font = new FontFace(fontName, `url(${fontUrl})`)
  
  try {
    await font.load()
    document.fonts.add(font)
    return true
  } catch (error) {
    console.error('Font loading failed:', error)
    return false
  }
}

// 使用示例
import customFontUrl from '@/assets/fonts/custom-font.woff2'
import { loadFont } from '@/utils/fontLoader'

async function initFonts() {
  await loadFont('CustomFont', customFontUrl)
}
```

## CSS 资源处理

### CSS 模块
```css
/* src/components/Button.module.css */
.button {
  background: var(--primary-color);
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.button:hover {
  background: var(--primary-hover);
  transform: translateY(-1px);
}

.primary {
  background: #007bff;
}

.secondary {
  background: #6c757d;
}
```

```javascript
// src/components/Button.jsx
import styles from './Button.module.css'
import clsx from 'clsx'

function Button({ variant = 'primary', children, ...props }) {
  return (
    <button 
      className={clsx(styles.button, styles[variant])}
      {...props}
    >
      {children}
    </button>
  )
}
```

### CSS 预处理器
```scss
// src/styles/variables.scss
$primary-color: #007bff;
$secondary-color: #6c757d;
$border-radius: 6px;
$transition-duration: 0.2s;

// 混合宏
@mixin button-style($bg-color) {
  background: $bg-color;
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: $border-radius;
  cursor: pointer;
  transition: all $transition-duration ease;
  
  &:hover {
    background: darken($bg-color, 10%);
    transform: translateY(-1px);
  }
}
```

```scss
// src/components/Button.scss
@import '../styles/variables';

.button {
  @include button-style($primary-color);
  
  &.secondary {
    @include button-style($secondary-color);
  }
}
```

## JSON 和数据文件

### JSON 文件导入
```javascript
// 导入整个 JSON 文件
import config from './config.json'
import packageInfo from '../package.json'

// 导入 JSON 的特定字段
import { version } from '../package.json'

// 使用示例
function AppInfo() {
  return (
    <div>
      <h1>{config.appName}</h1>
      <p>Version: {packageInfo.version}</p>
    </div>
  )
}
```

### 动态 JSON 加载
```javascript
// src/utils/dataLoader.js
export async function loadConfig(environment) {
  try {
    const config = await import(`../config/${environment}.json`)
    return config.default
  } catch (error) {
    console.error('Config loading failed:', error)
    return {}
  }
}

// 使用示例
const config = await loadConfig('production')
```

## Web Workers

### 基础 Worker 使用
```javascript
// src/workers/calculator.js
self.onmessage = function(e) {
  const { operation, numbers } = e.data
  
  let result
  switch (operation) {
    case 'add':
      result = numbers.reduce((sum, num) => sum + num, 0)
      break
    case 'multiply':
      result = numbers.reduce((product, num) => product * num, 1)
      break
    default:
      result = 0
  }
  
  self.postMessage({ result })
}
```

```javascript
// src/components/Calculator.jsx
import CalculatorWorker from '../workers/calculator.js?worker'

function Calculator() {
  const [result, setResult] = useState(0)
  const workerRef = useRef()
  
  useEffect(() => {
    workerRef.current = new CalculatorWorker()
    
    workerRef.current.onmessage = (e) => {
      setResult(e.data.result)
    }
    
    return () => {
      workerRef.current?.terminate()
    }
  }, [])
  
  const calculate = (operation, numbers) => {
    workerRef.current?.postMessage({ operation, numbers })
  }
  
  return (
    <div>
      <button onClick={() => calculate('add', [1, 2, 3, 4, 5])}>
        Calculate Sum
      </button>
      <p>Result: {result}</p>
    </div>
  )
}
```

### 共享 Worker
```javascript
// src/workers/shared-worker.js
const connections = []

self.onconnect = function(e) {
  const port = e.ports[0]
  connections.push(port)
  
  port.onmessage = function(e) {
    // 广播消息给所有连接
    connections.forEach(conn => {
      if (conn !== port) {
        conn.postMessage(e.data)
      }
    })
  }
}
```

## 资源优化策略

### 资源内联配置
```javascript
// vite.config.js
export default defineConfig({
  build: {
    assetsInlineLimit: 4096, // 4kb 以下内联
    
    rollupOptions: {
      output: {
        assetFileNames: (assetInfo) => {
          const info = assetInfo.name.split('.')
          const ext = info[info.length - 1]
          
          // 根据文件类型分类存放
          if (/\.(png|jpe?g|gif|svg|webp)$/i.test(assetInfo.name)) {
            return `images/[name]-[hash].${ext}`
          }
          if (/\.(woff2?|eot|ttf|otf)$/i.test(assetInfo.name)) {
            return `fonts/[name]-[hash].${ext}`
          }
          if (/\.(css)$/i.test(assetInfo.name)) {
            return `styles/[name]-[hash].${ext}`
          }
          return `assets/[name]-[hash].${ext}`
        }
      }
    }
  }
})
```

### 资源压缩
```javascript
import { defineConfig } from 'vite'
import { ViteImageOptimize } from 'vite-plugin-imagemin'
import { compression } from 'vite-plugin-compression'

export default defineConfig({
  plugins: [
    // 图片压缩
    ViteImageOptimize({
      gifsicle: { optimizationLevel: 7 },
      mozjpeg: { quality: 80 },
      pngquant: { quality: [0.65, 0.8] }
    }),
    
    // Gzip 压缩
    compression({
      algorithm: 'gzip',
      ext: '.gz'
    }),
    
    // Brotli 压缩
    compression({
      algorithm: 'brotliCompress',
      ext: '.br'
    })
  ]
})
```

## 公共资源目录

### public 目录使用
```
public/
├── favicon.ico
├── robots.txt
├── manifest.json
├── images/
│   ├── og-image.png
│   └── app-icon.png
└── data/
    └── config.json
```

```javascript
// 访问 public 目录中的资源
function App() {
  return (
    <div>
      {/* 直接引用 public 目录中的文件 */}
      <link rel="icon" href="/favicon.ico" />
      <img src="/images/og-image.png" alt="OG Image" />
      
      {/* 动态加载 public 目录中的数据 */}
      <script>
        fetch('/data/config.json')
          .then(res => res.json())
          .then(config => console.log(config))
      </script>
    </div>
  )
}
```

## 故障排除

### 常见资源问题
```javascript
// 1. 资源路径问题
// ❌ 错误的相对路径
import logo from '../../../assets/logo.png'

// ✅ 使用别名
import logo from '@/assets/logo.png'

// 2. 动态导入路径问题
// ❌ 完全动态的路径
const module = await import(dynamicPath)

// ✅ 部分静态路径
const module = await import(`./modules/${moduleName}.js`)

// 3. 资源未找到问题
// 检查文件是否存在
import.meta.glob('./assets/images/*.png', { eager: true })
```

### 资源加载错误处理
```javascript
// 图片加载错误处理
function ImageWithFallback({ src, fallback, alt, ...props }) {
  const [imgSrc, setImgSrc] = useState(src)
  
  const handleError = () => {
    setImgSrc(fallback)
  }
  
  return (
    <img 
      src={imgSrc} 
      alt={alt} 
      onError={handleError}
      {...props} 
    />
  )
}
```

## 总结

Vite 的静态资源处理提供了：
- 多种资源导入方式
- 强大的资源优化能力
- 灵活的资源组织策略
- 完善的错误处理机制

掌握这些资源处理技巧，能够帮助你构建更高效、更优化的前端应用。