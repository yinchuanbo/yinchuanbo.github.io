---
title: 枚举 (Enums)
category: TypeScript
date: 2025-07-30 17:41:57
---

## 什么是枚举？

枚举（Enum）是 TypeScript 中的一种数据类型，它允许我们定义一组命名的常量。枚举使代码更加可读和可维护，特别适用于表示一组相关的常量值。

## 数字枚举

### 基本数字枚举

```typescript
// 基本数字枚举
enum Direction {
    Up,    // 0
    Down,  // 1
    Left,  // 2
    Right  // 3
}

console.log(Direction.Up);    // 0
console.log(Direction.Down);  // 1
console.log(Direction.Left);  // 2
console.log(Direction.Right); // 3

// 使用枚举
function move(direction: Direction) {
    switch (direction) {
        case Direction.Up:
            console.log("Moving up");
            break;
        case Direction.Down:
            console.log("Moving down");
            break;
        case Direction.Left:
            console.log("Moving left");
            break;
        case Direction.Right:
            console.log("Moving right");
            break;
    }
}

move(Direction.Up); // "Moving up"
```

### 自定义数字枚举

```typescript
// 指定起始值
enum Status {
    Pending = 1,
    Approved,    // 2
    Rejected,    // 3
    Cancelled    // 4
}

// 指定所有值
enum HttpStatus {
    OK = 200,
    NotFound = 404,
    InternalServerError = 500
}

// 混合指定
enum Priority {
    Low = 1,
    Medium,      // 2
    High = 10,
    Critical     // 11
}

console.log(Priority.Low);      // 1
console.log(Priority.Medium);   // 2
console.log(Priority.High);     // 10
console.log(Priority.Critical); // 11
```

### 反向映射

```typescript
enum Color {
    Red,
    Green,
    Blue
}

console.log(Color.Red);   // 0
console.log(Color[0]);    // "Red"
console.log(Color[1]);    // "Green"
console.log(Color[2]);    // "Blue"

// 枚举对象的完整结构
console.log(Color);
// {
//   0: "Red",
//   1: "Green", 
//   2: "Blue",
//   Red: 0,
//   Green: 1,
//   Blue: 2
// }
```

## 字符串枚举

### 基本字符串枚举

```typescript
// 字符串枚举
enum Direction {
    Up = "UP",
    Down = "DOWN",
    Left = "LEFT",
    Right = "RIGHT"
}

console.log(Direction.Up);   // "UP"
console.log(Direction.Down); // "DOWN"

// 字符串枚举没有反向映射
// console.log(Direction["UP"]); // undefined
```

### 实际应用示例

```typescript
// API 端点枚举
enum ApiEndpoint {
    Users = "/api/users",
    Products = "/api/products",
    Orders = "/api/orders",
    Auth = "/api/auth"
}

// 事件类型枚举
enum EventType {
    Click = "click",
    Hover = "hover",
    Focus = "focus",
    Blur = "blur"
}

// 用户角色枚举
enum UserRole {
    Admin = "admin",
    User = "user",
    Guest = "guest",
    Moderator = "moderator"
}

// 使用示例
function handleEvent(type: EventType) {
    switch (type) {
        case EventType.Click:
            console.log("Handling click event");
            break;
        case EventType.Hover:
            console.log("Handling hover event");
            break;
        default:
            console.log("Unknown event type");
    }
}

handleEvent(EventType.Click); // "Handling click event"
```

## 异构枚举

```typescript
// 混合数字和字符串的枚举（不推荐）
enum Mixed {
    No = 0,
    Yes = "YES"
}

console.log(Mixed.No);  // 0
console.log(Mixed.Yes); // "YES"

// 更实际的异构枚举示例
enum Response {
    Success = 1,
    Error = "ERROR",
    Loading = "LOADING"
}
```

## 常量枚举

### const 枚举

```typescript
// 常量枚举
const enum Directions {
    Up,
    Down,
    Left,
    Right
}

// 编译后会被内联
let directions = [
    Directions.Up,    // 编译后变成 0
    Directions.Down,  // 编译后变成 1
    Directions.Left,  // 编译后变成 2
    Directions.Right  // 编译后变成 3
];

// 编译后的 JavaScript 代码：
// let directions = [0, 1, 2, 3];
```

### 常量枚举的限制

```typescript
const enum ConstEnum {
    A,
    B,
    C
}

// 可以使用
let a = ConstEnum.A;

// 不能使用（编译错误）
// let enumObject = ConstEnum; // Error
// let keys = Object.keys(ConstEnum); // Error
```

## 计算成员和常量成员

### 常量成员

```typescript
enum FileAccess {
    // 常量成员
    None,
    Read = 1 << 1,
    Write = 1 << 2,
    ReadWrite = Read | Write,
    
    // 计算成员
    G = "123".length
}

console.log(FileAccess.None);      // 0
console.log(FileAccess.Read);      // 2
console.log(FileAccess.Write);     // 4
console.log(FileAccess.ReadWrite); // 6
console.log(FileAccess.G);         // 3
```

### 位标志枚举

```typescript
// 位标志枚举
enum Permission {
    None = 0,
    Read = 1 << 0,    // 1
    Write = 1 << 1,   // 2
    Execute = 1 << 2, // 4
    Delete = 1 << 3,  // 8
    
    // 组合权限
    ReadWrite = Read | Write,           // 3
    ReadExecute = Read | Execute,       // 5
    All = Read | Write | Execute | Delete // 15
}

// 权限检查函数
function hasPermission(userPermissions: Permission, requiredPermission: Permission): boolean {
    return (userPermissions & requiredPermission) === requiredPermission;
}

// 添加权限
function addPermission(current: Permission, newPermission: Permission): Permission {
    return current | newPermission;
}

// 移除权限
function removePermission(current: Permission, permissionToRemove: Permission): Permission {
    return current & ~permissionToRemove;
}

// 使用示例
let userPerms = Permission.Read | Permission.Write;
console.log(hasPermission(userPerms, Permission.Read));  // true
console.log(hasPermission(userPerms, Permission.Execute)); // false

userPerms = addPermission(userPerms, Permission.Execute);
console.log(hasPermission(userPerms, Permission.Execute)); // true

userPerms = removePermission(userPerms, Permission.Write);
console.log(hasPermission(userPerms, Permission.Write)); // false
```

## 枚举与类型

### 枚举作为类型

```typescript
enum Color {
    Red,
    Green,
    Blue
}

// 枚举可以作为类型使用
function paintCar(color: Color): void {
    console.log(`Painting car with color: ${Color[color]}`);
}

paintCar(Color.Red);   // "Painting car with color: Red"
// paintCar(5);        // Error: 类型不匹配

// 枚举成员类型
type PrimaryColor = Color.Red | Color.Green | Color.Blue;
```

### 字面量枚举

```typescript
// 字面量枚举
enum ShapeKind {
    Circle,
    Square
}

interface Circle {
    kind: ShapeKind.Circle;
    radius: number;
}

interface Square {
    kind: ShapeKind.Square;
    sideLength: number;
}

type Shape = Circle | Square;

function getArea(shape: Shape): number {
    switch (shape.kind) {
        case ShapeKind.Circle:
            return Math.PI * shape.radius ** 2;
        case ShapeKind.Square:
            return shape.sideLength ** 2;
        default:
            // TypeScript 知道这里永远不会执行
            const _exhaustiveCheck: never = shape;
            return _exhaustiveCheck;
    }
}
```

## 实际应用示例

### 状态机

```typescript
// 订单状态枚举
enum OrderStatus {
    Pending = "pending",
    Processing = "processing",
    Shipped = "shipped",
    Delivered = "delivered",
    Cancelled = "cancelled",
    Returned = "returned"
}

// 状态转换映射
const statusTransitions: Record<OrderStatus, OrderStatus[]> = {
    [OrderStatus.Pending]: [OrderStatus.Processing, OrderStatus.Cancelled],
    [OrderStatus.Processing]: [OrderStatus.Shipped, OrderStatus.Cancelled],
    [OrderStatus.Shipped]: [OrderStatus.Delivered, OrderStatus.Returned],
    [OrderStatus.Delivered]: [OrderStatus.Returned],
    [OrderStatus.Cancelled]: [],
    [OrderStatus.Returned]: []
};

class Order {
    constructor(
        public id: string,
        public status: OrderStatus = OrderStatus.Pending
    ) {}

    canTransitionTo(newStatus: OrderStatus): boolean {
        return statusTransitions[this.status].includes(newStatus);
    }

    transitionTo(newStatus: OrderStatus): void {
        if (this.canTransitionTo(newStatus)) {
            console.log(`Order ${this.id}: ${this.status} -> ${newStatus}`);
            this.status = newStatus;
        } else {
            throw new Error(`Cannot transition from ${this.status} to ${newStatus}`);
        }
    }
}

// 使用示例
const order = new Order("ORD-001");
order.transitionTo(OrderStatus.Processing); // 成功
order.transitionTo(OrderStatus.Shipped);    // 成功
// order.transitionTo(OrderStatus.Pending);  // 错误：不能从 shipped 转换到 pending
```

### 配置管理

```typescript
// 环境枚举
enum Environment {
    Development = "development",
    Testing = "testing",
    Staging = "staging",
    Production = "production"
}

// 日志级别枚举
enum LogLevel {
    Debug = 0,
    Info = 1,
    Warn = 2,
    Error = 3
}

// 配置接口
interface Config {
    environment: Environment;
    logLevel: LogLevel;
    apiUrl: string;
    enableDebug: boolean;
}

// 配置工厂
class ConfigFactory {
    static create(env: Environment): Config {
        const baseConfig = {
            environment: env,
            logLevel: LogLevel.Info,
            apiUrl: "",
            enableDebug: false
        };

        switch (env) {
            case Environment.Development:
                return {
                    ...baseConfig,
                    logLevel: LogLevel.Debug,
                    apiUrl: "http://localhost:3000/api",
                    enableDebug: true
                };
            
            case Environment.Testing:
                return {
                    ...baseConfig,
                    logLevel: LogLevel.Debug,
                    apiUrl: "http://test-api.example.com/api",
                    enableDebug: true
                };
            
            case Environment.Staging:
                return {
                    ...baseConfig,
                    logLevel: LogLevel.Warn,
                    apiUrl: "http://staging-api.example.com/api",
                    enableDebug: false
                };
            
            case Environment.Production:
                return {
                    ...baseConfig,
                    logLevel: LogLevel.Error,
                    apiUrl: "https://api.example.com/api",
                    enableDebug: false
                };
            
            default:
                throw new Error(`Unsupported environment: ${env}`);
        }
    }
}

// 使用示例
const config = ConfigFactory.create(Environment.Development);
console.log(config);
```

## 枚举的最佳实践

### 1. 优先使用字符串枚举

```typescript
// 推荐：字符串枚举
enum Theme {
    Light = "light",
    Dark = "dark",
    Auto = "auto"
}

// 不推荐：数字枚举（除非有特殊需求）
enum ThemeNumeric {
    Light,  // 0
    Dark,   // 1
    Auto    // 2
}
```

### 2. 使用 const 枚举优化性能

```typescript
// 对于不需要反向映射的枚举，使用 const
const enum HttpMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE"
}

// 编译后会被内联，减少运行时开销
```

### 3. 枚举命名规范

```typescript
// 好的命名
enum UserStatus {
    Active = "active",
    Inactive = "inactive",
    Suspended = "suspended"
}

enum PaymentMethod {
    CreditCard = "credit_card",
    PayPal = "paypal",
    BankTransfer = "bank_transfer"
}

// 避免过于简短或模糊的命名
enum Status {  // 太模糊
    A = "a",   // 不清楚含义
    B = "b"
}
```

### 4. 枚举与联合类型的选择

```typescript
// 当值是固定的字符串时，可以考虑使用联合类型
type Theme = "light" | "dark" | "auto";

// 当需要更多功能（如反向映射、位运算）时，使用枚举
enum Permission {
    Read = 1,
    Write = 2,
    Execute = 4
}
```

## 总结

枚举是 TypeScript 中组织相关常量的强大工具：

1. **数字枚举**：默认从 0 开始，支持反向映射
2. **字符串枚举**：更加明确和可读，推荐使用
3. **常量枚举**：编译时内联，性能更好
4. **位标志枚举**：适用于权限系统等场景

选择合适的枚举类型可以让代码更加清晰、类型安全和易于维护。

## 练习题

1. 创建一个表示一周七天的枚举，并实现一个函数判断是否为工作日
2. 设计一个游戏状态枚举，包含状态转换逻辑
3. 实现一个基于枚举的简单权限系统
4. 创建一个 HTTP 状态码枚举，并实现相应的错误处理函数