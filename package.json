{
  "name": "hao-md",
  "version": "1.0.0",
  "description": "A markdown blog system",
  "scripts": {
    "dev": "node server.js",
    "start": "serve public",
    "build": "echo 'Build complete'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "front-matter": "^4.0.2",
    "fs-extra": "^11.3.0",
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=16.x"
  }
}
