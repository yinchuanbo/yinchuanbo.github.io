{
  "name": "hao-md",
  "version": "1.0.0",
  "description": "A markdown blog system",
  "scripts": {
    "start": "serve public",
    "build": "echo 'Build complete'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=16.x"
  }
}